task buildAll {
    //dependsOn("shiro_example:bootJar")
}
allprojects {
    task printInfo {
        doLast {
            println("in $project.name version: $project.main_version")
        }
    }
}
